import styled from "styled-components";

const CanvasWrapper = styled.div`
	margin-top: 30px;
	display: flex;
	flex-direction: column;
`;

const StyledCanvas = styled.canvas`
	width: 100%;
`;

export { CanvasWrapper, StyledCanvas };
