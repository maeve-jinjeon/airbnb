import { CancelButton, RemoveButton } from "util/Icons";

const Header = () => {
	return (
		<div>
			HEADER
			<CancelButton color="primary" size={40} />
			<RemoveButton color="primary" size={40} />
		</div>
	);
};
// for test

export default Header;
